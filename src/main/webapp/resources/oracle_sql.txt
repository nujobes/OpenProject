CREATE SEQUENCE op_idx_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE OP_MEMBER 
(
  IDX NUMBER NOT NULL 
, USERID VARCHAR2(20) NOT NULL 
, PASSWORD VARCHAR2(20) NOT NULL 
, USERNAME VARCHAR2(50) NOT NULL 
, USERPHOTO VARCHAR2(250) DEFAULT 'no-image'
, CONSTRAINT OP_MEMBER_PK PRIMARY KEY 
  (
    USERID 
  )
);


INSERT INTO op_member VALUES (
    1,
    'tester',
    'tester',
    'ȫ�浿',
    ''
);

CREATE SEQUENCE op_gb_idx_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE OP_GUESTBOOK 
(
  IDX NUMBER NOT NULL 
, USERID VARCHAR2(20) NOT NULL 
, MESSAGE VARCHAR2(250) NOT NULL 
, CONSTRAINT OP_GUESTBOOK_PK PRIMARY KEY 
  (
    IDX 
  )
  ENABLE 
);
